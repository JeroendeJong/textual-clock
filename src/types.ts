
type ClockLocale = {
  HOUR_NUMBERS: { [key: string]: string },
  MINUTE_NUMBERS: { [key: string]: string },
  WHOLE_HOUR_SUFFIX: string,
  QUARTER: string,
  HALF: string
  PREPOSITION_BEFORE: string
  PREPOSITION_AFTER: string
  GRID_LENGTH: number
}

type Rectangle = {
  width: number,
  height: number,
  id: number
}